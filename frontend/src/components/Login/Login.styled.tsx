import { RiUserLine } from 'react-icons/ri';
import styled from 'styled-components';

export const UserIcon = styled(RiUserLine)`
  color: var(--brown-icon);
  width: 20px;
  height: 28px;
  stroke-width: 0.2px;
`;
export const List = styled('ul')`
  display: flex;
  align-items: center;
  gap: 10px;
`;
